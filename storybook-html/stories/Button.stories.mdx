import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs/blocks";

export const Template = ({ text, btnType, focus }) => {
  return `
    <button class="btn ${btnType === "default" ? "" : btnType} ${
    focus ? "has-focus" : ""
  } ">${text}</button>
  `;
};

<Meta
  title="Button"
  argTypes={{
    text: {
      control: {
        type: "text",
      },
    },
    btnType: {
      control: {
        type: "select",
        options: ["default", "primary"],
      },
    },
    focus: {
      control: {
        type: "boolean",
      },
    },
  }}
/>

# Button

<Canvas>
  <Story name="Button" args={{ text: "Change me", btnType: "default" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## ArgsTable

<ArgsTable story="Button" />
